<!-- 
    ====================================================================================================================
    Fragmento de Diálogo: Criar Role (CreateRoleDialog.fragment.xml)
    ====================================================================================================================
    Este fragmento define um sap.m.Dialog para a criação de uma nova Role.

    - É usado na view `Roles` para obter os dados de uma nova Role do utilizador.
    - Contém um `SimpleForm` com campos de input para o nome e a descrição.
    - Os campos de input estão ligados a um modelo JSON local chamado `newRole`, que é criado e definido no controller
      sempre que o diálogo é aberto. Este modelo armazena temporariamente os dados inseridos.
    - Os botões `Save` e `Cancel` acionam os métodos `onSaveNewRole` and `onCancelNewRole` no controller (`Roles.controller.ts`)
      para guardar a nova entidade ou fechar o diálogo.
    ====================================================================================================================
-->
<core:FragmentDefinition
        xmlns="sap.m"
        xmlns:core="sap.ui.core"
        xmlns:form="sap.ui.layout.form">
    <Dialog id="createRoleDialog" title="Create Role">
        <content>
            <form:SimpleForm editable="true">
                <Label text="Name" required="true"/>
                <Input value="{newRole>/name}"/>
                <Label text="Description" required="true"/>
                <Input value="{newRole>/description}"/>
            </form:SimpleForm>
        </content>
        <beginButton>
            <Button text="Save" type="Emphasized" press=".onSaveNewRole"/>
        </beginButton>
        <endButton>
            <Button text="Cancel" press=".onCancelNewRole"/>
        </endButton>
    </Dialog>
</core:FragmentDefinition>